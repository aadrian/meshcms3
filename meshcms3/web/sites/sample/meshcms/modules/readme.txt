According to the default configuration, this folder contains custom module templates. Module templates are JSP files that can process the file(s) included in a module and create a block to be inserted in website pages. Writing a custom module template is a task that requires some Java programming skill.

This file can be safely deleted.
